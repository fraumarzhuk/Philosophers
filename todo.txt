TODO:

1. void create_threads()
{
	while (i <= philo_info->num_philos)
		init all the threads;
		add all the indexes;
		philo->index = 1;
		current = current->next
}
2. Create and join the threads.
	while (current->next)
		pthread_create()
3. Create a function, that checks simultaneously what is the state of the other threads and picks the action, calls the function that prints the state and changes the state. 
4. void print_state()
	checkd the state and prints out the message
